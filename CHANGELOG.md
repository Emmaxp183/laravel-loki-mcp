# Changelog

## 0.1.0

- Added the initial Laravel MCP Suite package structure on top of `laravel/mcp`
- Added Laravel-native tools, resources, and prompt packs
- Added audit logging and safe Artisan execution policy
- Added `mcp:install` scaffolding for config, routes, and project conventions
- Added optional protected web transport with shared-token auth
- Added a real demo Laravel app and setup documentation for `Codex CLI` and `Claude Code`
